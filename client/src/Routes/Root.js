import Login from './Login';
import Lougout from './Logout';

const Root = () => {
    // Some logic to determine if user is logged in
    // If true: 
    return (
        <Login />
    )
}

export default Root;